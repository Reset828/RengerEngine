dzc_add_interface_target(dzc_app)

# Qt5 Widgets discovery and linking are intentionally deferred until the
# concrete application implementation. This is the only target allowed to
# link Qt Widgets.