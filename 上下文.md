# Dzc-RenderEngine 上下文交接

> 最后整理：2026-09-05
> 当前工作目录：`D:\projects\Dzc-RengerEngine`  
> 本文件用于新会话交接。开始下一项工作前，先阅读 `agent.md`、本文件、`docs/tasks/qt-application.md` 与 `docs/tasks/progress.md`。

## 1. 当前目标与项目状态

项目是 GIS 海量点云可视化引擎。当前 OpenGL Renderer 模块处于**已完成**状态：

- GL-001 至 GL-012 已完成；
- GL-012 已覆盖规范化 Chunk 到注入式 OpenGL 后端渲染的完整链路；
- OpenGL Renderer 四项模块级验收均已通过；
- 整个 Phase 1 阶段门禁仍未完成，不得提前标记 Phase 1 完成。

任务状态的唯一记录位置是：

- `docs/tasks/opengl-renderer.md`
- `docs/tasks/progress.md`

## 2. 当前 Git 状态（重要）

最近提交：

```text
d70ceb8 实现命令行配置解析
7c2e7d6 配置 Qt 5.15.19 App Target
b31f2fe 完成 OpenGL 后端集成测试
cbd2fc7 实现 GPU Timer Query
f56f83f 实现 resize、viewport 与投影适配
```

当前 HEAD 以实际 Git 为准：`d70ceb8`（QT-002 命令行配置解析）。GL-011 为 `cbd2fc7`，GL-012 为 `b31f2fe`，QT-001 为 `7c2e7d6`；本次 QT-003/QT-004 不创建或修改 Git commit。当前工作树改动以实际 `git status` 为准，`上下文.md` 仍是未跟踪但必须保留的交接文件。任务专用 `build-qt003` 应在收尾时安全删除，不得为获得干净状态而删除本文件。

当前预计工作树改动包括：

```text
M  docs/design/detailDesign.md
M  docs/tasks/progress.md
M  docs/tasks/qt-application.md
M  src/app/CMakeLists.txt
M  src/app/CommandLineOptions.cpp
M  src/app/CommandLineOptions.h
M  tests/unit/CMakeLists.txt
?? src/app/ApplicationConfig.h
?? src/app/SettingsController.cpp
?? src/app/SettingsController.h
?? tests/unit/SettingsControllerTests.cpp
?? 上下文.md
```

## 3. QT-001 与 CUDA 暂缓
截至 2026-09-03，已执行 `qt-application.md` 的 QT-001：

- `src/app/CMakeLists.txt` 使用 Qt 5.15.19 `Widgets`，`dzc_app` 已转为私有链接 Qt 的 executable；`src/app/main.cpp` 仅创建标题为 `Dzc-RenderEngine` 的标准空 `QMainWindow`。
- 新增独立 `tests/ui/CMakeLists.txt` 和 `tests/ui/QtApplicationSmokeTests.cpp`，注册 `dzc_qt_application_smoke`，环境为 `QT_QPA_PLATFORM=offscreen`，标签为 `ui;qt;qt001`；不链接或嵌入 `dzc_app`。
- QT-002 至 QT-012、Qt Application 模块级验收和 Phase 1 阶段门禁均保持未完成。
- CUDA-OpenGL Interop 与 CUDA-Vulkan Interop 按主人要求本阶段暂缓/跳过：不执行 CUDA Target、设备匹配、Interop、Kernel、同步或验收测试；不勾选 CUDA 任务及 `AC-P1-011`/`AC-P1-012`，统一使用 `DZC_ENABLE_CUDA=OFF`。待 Qt/Phase 1 当前工作完成后，由主人明确重新启动 CUDA。
- 按 `agent.md`，运行 Qt 测试前必须检查仓库根目录 `Dzc-RengerEngine\dll`；若缺少测试所需 DLL，停止运行并报告，不得从 Qt 安装目录、其他构建目录或 PATH 复制。
- QT-001 当前验证：`build-qt001` 使用 Qt 5.15.19、OpenGL ON、Vulkan OFF、CUDA OFF、Tests ON 和项目 NMake/vcpkg/PCL 配置成功生成；`dzc_app` 与 `dzc_qt_application_smoke` 构建成功，`dzc_target_boundary` 通过。
- 按 `agent.md` 检查仓库根目录 `dll` 后，发现缺少 Qt 运行时 DLL（至少 Qt5Core/Qt5Gui/Qt5Widgets 及其运行依赖），因此未运行 smoke CTest 或其他 CTest；不得从 Qt 安装目录、其他构建目录或 PATH 复制 DLL 规避。
- `build-qt001` 为任务专用临时构建目录，收尾时应安全删除；不创建 Git commit。
## 4. QT-002 命令行配置解析

截至 2026-09-03，已执行 `qt-application.md` 的 QT-002：

- 新增 App 内部 `src/app/CommandLineOptions.h` 与 `src/app/CommandLineOptions.cpp`，使用 `--name=value` 严格解析 backend、cuda、log-level、cache-directory、CPU/GPU 十进制字节预算。
- `EngineConfig` 未修改；日志级别保存在 `CommandLineOptions::Parsed` 内部结果中。解析器不接入 `main.cpp` 或 Engine，QT-001 的空窗口行为保持不变。
- 新增 `tests/unit/CommandLineOptionsTests.cpp`，注册 `dzc_command_line_options`，覆盖默认值、完整参数、UTF-8 路径、最大 uint64、非法值、重复参数、未知参数、位置参数和 usage 诊断。
- `build-qt002` 已按 Qt 5.15.19、OpenGL ON、Vulkan/CUDA OFF、Tests ON 和项目 NMake/vcpkg/PCL 配置成功；`dzc_app`、`dzc_qt_application_smoke` 与 `dzc_command_line_options_tests` 构建成功，`dzc_command_line_options`、`dzc_target_boundary` 与既有 `dzc_result`、`dzc_engine_config`、`dzc_log_types` 回归测试通过。`dzc_app` 和纯 C++ 测试未发现 CUDA/cudart 依赖。纯 C++ QT-002 测试不需要 Qt DLL；Qt smoke 未运行，因为根目录 `dll/` 缺少 Qt 运行时 DLL，遵守 `agent.md` 未从其他位置复制。
- QT-003 至 QT-012、Qt Application 模块级验收和 Phase 1 阶段门禁仍保持未完成。
- CUDA-OpenGL Interop 与 CUDA-Vulkan Interop 继续按主人要求暂缓/跳过：不执行 CUDA Target、设备匹配、Interop、Kernel、同步或验收测试；不勾选 CUDA 任务及 `AC-P1-011`/`AC-P1-012`，统一使用 `DZC_ENABLE_CUDA=OFF`。待 Qt/Phase 1 当前工作完成后，由主人明确重新启动 CUDA。

## 5. QT-003 QSettings/INI 控制器

截至 2026-09-03，已执行 `qt-application.md` 的 QT-003：

- 新增 `src/app/ApplicationConfig.h`，定义仅供 App 使用的 `AppConfig`、presence-aware `AppConfigOverrides` 和 `SettingsLoadResult`；`EngineConfig` 公共结构未修改，日志级别仅保存在 App 内部。
- 新增 `src/app/SettingsController.h/.cpp`，以显式 `QSettings(iniPath, QSettings::IniFormat)` 读写 `engine/backend`、`engine/cuda`、`engine/logLevel`、`cache/directory`、`memory/gpuCacheBytes` 和 `memory/cpuCacheBytes`；不持久化 `cache.enabled`、线程数或队列容量，不创建目录。
- 缺失文件/键采用默认值且无 warning；空缓存目录合法；单个枚举或预算损坏时逐字段回退并通过 `SettingsLoadResult::warnings` 返回具体原因；INI 读错误映射为 `FileIo/1`，写入或 `sync()` 错误映射为 `FileIo/3`。
- `loadWithCommandLine()` 的优先级为“命令行 > INI > 默认值”，只覆盖实际出现的命令行字段，并保留 INI warnings；非法命令行返回 `Configuration/1`，不返回部分合并结果。
- 新增 `tests/unit/SettingsControllerTests.cpp` 并注册 `dzc_settings_controller`（`unit;app;qt;qt003`），覆盖默认值、往返、UTF-8、损坏值、错误和命令行覆盖；Qt 依赖只存在于 App/测试目标，Engine、Render、Compute、Data 不依赖 Qt/QSettings。
- `build-qt003` 配置成功；`dzc_settings_controller_tests` 和 `dzc_app` 目标构建成功，`dzc_target_boundary` 通过。运行 Qt 测试前检查仓库 `dll/` 发现缺少 Qt5Core 及相关 Qt 运行时 DLL，按 `agent.md` 停止，未从其他路径复制 DLL 或伪造通过。
- QT-004 至 QT-012、Qt Application 模块级验收和 Phase 1 阶段门禁保持未完成。
- CUDA-OpenGL Interop 与 CUDA-Vulkan Interop 继续按主人要求暂缓/跳过：不执行 CUDA Target、设备匹配、Interop、Kernel、同步或验收测试；不勾选 CUDA 任务及 `AC-P1-011`/`AC-P1-012`，统一使用 `DZC_ENABLE_CUDA=OFF`。待 Qt/Phase 1 当前工作完成后，由主人明确重新启动 CUDA。

## 5. QT-004 MainWindow 布局

截至 2026-09-03，已执行 `qt-application.md` 的 QT-004：

- 新增 `src/app/MainWindow.h/.cpp`，使用 C++ + Pimpl 实现标题为 `Dzc-RenderEngine` 的 `QMainWindow`；`src/app/main.cpp` 已改为创建并显示 `dzc::MainWindow`。
- 主窗口包含普通 `renderViewPlaceholder` 中央视图占位、初始可见的 `datasetDock`、`renderParametersDock`、`logDock` 三个 Dock；数据集、参数、日志和状态栏均为静态占位，不执行文件 I/O、Engine、渲染或 CUDA 行为。
- 创建 `File`、`View`、`Help` 菜单、关键 QAction 和三个 Dock toggle action，均只建立并稳定命名，业务连接留给后续 QT-005 至 QT-012；没有创建 `QOpenGLWidget` 或 Vulkan `QWindow`。
- 新增独立 `tests/ui/MainWindowSmokeTests.cpp` 和 CTest `dzc_main_window_smoke`（`QT_QPA_PLATFORM=offscreen`，标签 `ui;qt;qt004`），验证窗口标题、中央占位、三个 Dock、菜单、动作、可见性及事件循环退出。
- `build-qt004` 已使用 Qt 5.15.19、OpenGL ON、Vulkan OFF、CUDA OFF、Tests ON 配置；`dzc_main_window_smoke`、`dzc_app` 构建成功，QT-004 smoke 与 `dzc_target_boundary` 通过。QT-001 至 QT-003 测试未在本构建目录重新构建，未记为本次回归通过。
- 仓库 `dll/Qt5Core.dll`、`Qt5Gui.dll`、`Qt5Widgets.dll` 已由主人确认保留，并用于 Qt 测试运行；不得删除或替换。CUDA-OpenGL/CUDA-Vulkan Interop 仍按主人要求暂缓，继续使用 `DZC_ENABLE_CUDA=OFF`，不勾选 CUDA 任务或 Phase 1 CUDA 门禁。
## 5.1 QT-004 MainWindow 布局与验证

QT-004 已完成。当前工作树新增 `MainWindow` 及独立 `dzc_main_window_smoke`，并保持 Qt 只在 App/UI 测试目标、CUDA 暂缓和 `DZC_ENABLE_CUDA=OFF` 约定。Qt 5.15.19 运行时 DLL `Qt5Core.dll`、`Qt5Gui.dll`、`Qt5Widgets.dll` 已由主人确认保留在仓库 `dll/`，本次 smoke 使用该目录运行时约定；未删除这些文件。构建目录中的 Qt 临时部署文件若存在，仅属于构建产物，收尾时清理；不从 Qt 安装目录、其他构建目录或 PATH 复制 DLL。
## 6. OpenGL 架构边界

### 公共与后端无关层

- `src/render/common/RenderBackendTypes.h`
  - `RenderFrame` 包含：`frameId`、`camera`、`size`、背景色、`pointSize`、`shadingMode`、`fixedColor`、`heightRange`、`intensityRange` 与按可见顺序排列的 `draws`。
  - `heightRange` / `intensityRange` 是 `glm::vec2`，含义为 `[min, max]`，默认 `{0.0F, 0.0F}`。
  - `DrawChunk` 包含 `chunkId`、`pointCount`、`relativeOrigin`、`schema`。
- `src/render/common/ShaderData.h`
  - `FrameData` 为 208 字节的 std140 CPU 表示，binding 为 0；
  - `ChunkData` 为 16 字节的 std430 单记录结构，binding 为 1；
  - `makeFrameData()` 行为在 `OpenGLBackend.cpp` 中；矩阵通过 `toColumnMajorArray()` 原样转为列主序数组。

公共头文件不得暴露 `GLuint`、GLAD 类型、OpenGL 句柄或平台 Context 类型。

### OpenGL 私有层

目录：`src/render/opengl/`

- `GlResource.*`、`GlBuffer.*`、`GlVertexArray.*`：以 Context 线程 token 约束的 RAII 资源；析构不抛，不能安全释放时标记 `releasePending`。
- `GlShaderProgram.*`：Shader 文件读取、编译、链接与错误诊断。
- `GlChunkResource.*`：Chunk VAO/VBO 上传与属性布局。
- `GlDrawOperations.*`：完全私有的 GL 调用操作表。`IGlDrawOperations` 现有：绘制 Buffer 绑定/上传/binding point、Program/uint uniform、viewport、清屏和 `GL_POINTS` 绘制。默认 GLAD 实现仅在 `.cpp` 中引用 GLAD。
- `GlTimerQueryPool.*`：固定 3 帧延迟的 GPU elapsed-time 查询池；查询未就绪时保留槽位并跳过新计时，不等待当前帧且不调用 `glFinish`。GLAD 类型和句柄只出现在 `.cpp` 的默认操作表。
- `OpenGLBackend.*`：完整生命周期、Chunk 管理、Frame 更新、逐 Chunk 绘制、着色回退、viewport/暂停和 GPU Timer Query 逻辑；通过 `latestGpuFrameMilliseconds()` 暴露后端无关的 `std::optional<double>`，不持有 `MetricsRegistry`。
- `OpenGLCapabilities.*`：OpenGL 4.5 Core 能力检查。

`src/render/opengl/CMakeLists.txt`：

- `dzc_render_opengl` 是静态库；GLAD include 与 `OpenGL::GL` 都是私有依赖；
- 通过私有编译定义 `DZC_OPENGL_SHADER_DIR="${PROJECT_SOURCE_DIR}/shaders/opengl"` 注入 Shader 目录；
- 不依赖 Qt、Vulkan、CUDA、PCL 或 `dzc_app`。

## 7. 已完成的关键行为（GL-005 至 GL-011）

### Shader 数据、上传和生命周期

- Frame UBO（binding 0）、单记录 Chunk SSBO（binding 1）、默认 point-cloud Shader 在 `OpenGLBackend::init()` 成功后持久持有。
- `init()` 必须由调用方保证 Context 已 current；Backend 加载 GLAD、检查 OpenGL 4.5 能力、创建绘制资源并读取：
  - `shaders/opengl/point_cloud.vert`
  - `shaders/opengl/point_cloud.frag`
- 失败时按 RAII 释放已创建资源；`shutdown()` 也要求当前 Context 与创建线程匹配。
- Chunk GPU 上传使用 position/color/intensity 属性布局；VAO/VBO 被 `GlChunkResource` 私有持有。

### GL-008：逐 Chunk `GL_POINTS` 绘制

`OpenGLBackend::render()` 在正常状态下：

1. 检查 Backend 已初始化、同线程、Context current、GLAD 已加载以及已有成功的 `update()`；
2. 预检查当前 `RenderFrame.draws` 内所有可见 Chunk：资源存在且有效、点数一致、可转换为 `GLsizei`；任意失败时不执行上传/clear/draw；
3. 上传 FrameData UBO 并绑定 binding 0；
4. clear 背景色，绑定 Program；
5. 严格按 `RenderFrame.draws` 顺序，为每个 Chunk 上传 `relativeOrigin` 到单记录 Chunk SSBO、绑定 binding 1、绑定 VAO、提交一次 `GL_POINTS`；
6. 只有整帧成功才替换 `drawCount` 和 `submittedPointCount`。失败帧保留上一次成功统计。

不可见、未列入 `RenderFrame.draws` 的已上传 Chunk 不绘制。空 draw 列表是成功帧，统计为零。

### GL-009：四种着色与点大小

Shader 位置：

- `shaders/opengl/point_cloud.vert`
- `shaders/opengl/point_cloud.frag`

已实现：

- `OriginalColor`、`FixedColor`、`Height`、`Intensity` 四种模式；
- `FrameData.pointSize` 写入 Vertex Shader 的 `gl_PointSize`；
- Height/Intensity 根据帧的 range 归一化；`min == max` 固定输出归一化值 `0.5`；
- 伪彩色带为蓝（0）—青（0.25）—绿（0.5）—黄（0.75）—红（1），中间线性插值；
- 每次 draw 通过私有 uniform `drawHasColor` / `drawHasIntensity` 传递当前属性可用性；
- OriginalColor 缺 Color、Intensity 模式缺 Intensity 时回退 `fixedColor`，每种属性缺失每个 Backend 生命周期只通过注入的 `dzc::diagnostics::ILogSink` 记录一次 Warn；日志包含模块、ChunkId、FrameId 与回退原因；
- `update()` 校验 pointSize、范围的有限性与 `min <= max`。

### GL-010：resize、viewport 与投影适配

核心实现位于 `OpenGLBackend.cpp`：

- `RenderSize.width/height` 是逻辑尺寸；物理 viewport 尺寸为：
  - `round(width * devicePixelRatio)`
  - `round(height * devicePixelRatio)`
- `init()` 在持久绘制资源创建成功后调用一次私有 `setViewport(0, 0, physicalWidth, physicalHeight)`；调用失败则清理资源并初始化失败。
- 有效非零 `resize()`：先验证 DPR、物理尺寸和 `GLsizei` 范围，再调用 viewport；成功后才写入 `m_renderSize` 并退出暂停。
- `resize()` 的 viewport 操作失败时，保留之前的逻辑尺寸、暂停状态和缓存帧。
- DPR 非有限/非正、逻辑尺寸非零而物理尺寸 round 后为 0、或物理尺寸超过 `GLsizei`，均返回 `ResizeFailed` 且保持状态。
- 宽或高为 0 且 DPR 有效时是“窗口最小化”暂停：成功、**不调用 viewport**、保留 GPU 资源、清除 `m_currentFrame`、设 `m_renderSuspended = true`。
- 暂停期间：
  - `update()` 返回 `UpdateFailed`，不缓存帧；
  - `render()` 成功返回，但不上传 UBO/SSBO、不 clear、不绑定 Program/VAO、不绘制，并将最近一次成功 render 统计更新为零 draw、零点数。
- 有效 resize 改变逻辑 `RenderSize` 时，旧帧被清除；恢复后调用方必须使用新逻辑尺寸调用 `ICameraController::matrices(RenderSize)` 并重新 `update()`。
- 非暂停的 `update()` 要求 `RenderFrame.size == OpenGLBackend::renderSize()`；不匹配返回 `UpdateFailed`，并保留已缓存的有效帧。
- Backend **不重算、也不转换** view/projection；直接上传 `RenderFrame.camera`。相机层使用 OpenGL `NegativeOneToOne` 深度约定，逻辑 aspect 只由逻辑宽/高决定，DPR 不参与相机投影。

### GL-011：GPU Timer Query

- `GlTimerQueryPool` 固定持有 3 个查询槽位；当前帧只 begin/end，只解析至少 3 帧前的槽位。
- 查询未就绪返回 `std::nullopt`，保留槽位并跳过新的计时，但正常绘制继续；全过程不调用 `glFinish`。
- `OpenGLBackend` 在 Context、GLAD 和能力检查成功后创建计时池；有效 render 先解析旧查询，再包围当前绘制；零尺寸暂停不伪造 GPU 时间。
- Backend 仅暴露 `latestGpuFrameMilliseconds()`，不依赖 `MetricsRegistry`；`PerformanceMetrics` 和 `PerformanceSnapshot` 均使用 `std::optional<double>`。
- 创建、begin、end、可用性、读取和释放失败沿用 Backend 错误语义；查询资源遵守 Context 线程、RAII、移动和 `noexcept` 析构约束。

## 8. 测试与最近验证

GL-012 在 `tests/integration/OpenGLRenderPipelineTests.cpp` 新增并注册：

```text
dzc_opengl_render_pipeline
dzc_opengl_render_pipeline_real_context
```

Fake 集成场景覆盖：

- 真实 `GridChunkBuilder` 将带 Position/Color/Intensity 的 3 点 `GridBucket` 构造成局部化的 `CpuResident Chunk`，再通过 `UploadBatch` 进入 `OpenGLBackend`；
- 三条 VBO、VAO location 0/1/2、FrameData UBO binding 0、ChunkData SSBO binding 1；
- OriginalColor、FixedColor、Height、Intensity 四帧，Fake 累计 4 次等价 `GL_POINTS` 绘制和 12 个提交点，Backend 每帧统计为 1 次绘制、3 个点；
- `320×240@1.0` resize 到 `640×360@1.5` 后物理 viewport 为 `960×540`；
- 固定三帧延迟 Timer Query 返回有限、非负的 2.5 ms；
- `release(chunkId)`、`shutdown()` 和 Shader 编译失败初始化回滚后无 Fake 资源残留。

真实 Context 用例不创建 Qt、WGL、EGL 或其他 headless/offscreen Context，输出明确限制原因并返回 77；CTest 设置 `SKIP_RETURN_CODE 77`，因此显示为 **Skipped**，不伪造真实渲染成功。该任务不包含 framebuffer 像素读取、截图或图像哈希。

2026-09-02 最近成功验证：

- GL-012 专项 2 项：Fake 用例通过，真实 Context 用例明确 Skipped；
- 完整 `opengl` 标签回归 22/22 无失败，其中 10 项真实 Context 用例按 77 明确 Skipped；
- 构建目录未发现或复制任何临时 DLL；运行测试未从仓库外路径引入 DLL。

构建使用 vcpkg 工具链 `D:\vcpkg\vcpkg\scripts\buildsystems\vcpkg.cmake`。执行测试仍必须遵守 `agent.md`：仅从仓库根目录 `dll/` 复制所需运行时 DLL；缺少 DLL 时停止并报告给主人；测试后清理构建测试目录内的临时 DLL，不得通过其他目录或 PATH 规避。

## 9. OpenGL Renderer 交付与后续任务

### GL-011：GPU Timer Query（已完成）

- 新增 `GlTimerQueryPool` 私有 GLAD 操作表和固定 3 帧延迟查询池；只查询至少 3 帧前的结果，不调用 `glFinish`，不等待当前帧。
- `PerformanceMetrics::gpuFrameMilliseconds` 与 `PerformanceSnapshot::gpuFrameMilliseconds` 现在都是 `std::optional<double>`；未就绪时清空，完成后仅写入非负、有限的毫秒结果。
- 查询未就绪保留槽位并跳过新计时，正常绘制继续；创建/开始/结束/可用性/读取/释放失败通过既有错误语义处理，查询生命周期仍受 Context 线程约束。
- OpenGL-only graphics CTest 19/19 通过；9 个真实 Context 用例按 77 约定 Skipped；MetricsRegistry 专项测试通过。未创建 Qt/WGL/EGL/真实 Context 设施。

### GL-012：OpenGL 后端集成测试（已完成）

- 新增 `tests/integration/OpenGLRenderPipelineTests.cpp` 与两项带 `integration;graphics;opengl;gl012` 标签的 CTest；真实 Context 用例额外带 `skipped` 标签。
- 使用注入式 Context/能力/绘制/Shader/Timer Query Fake 完成端到端管线验证，不修改公共 API。
- 四种着色、resize/DPR、UBO/SSBO、逐 Chunk 绘制、统计、三帧延迟 GPU 计时、显式释放、shutdown 与错误 Shader 回滚均已覆盖。
- 项目仍未提供 Qt/WGL/EGL/真实 headless/offscreen Context；相关测试按 77/Skipped 明确表达，不伪造通过。
- OpenGL Renderer 模块已完成，但 Phase 1 阶段门禁仍保持未完成。

## 10. 强制约束与继续开发清单

1. 每次回复开头称呼用户为“主人”。
2. 任何完成的子任务均同步更新：`docs/tasks/opengl-renderer.md`、`docs/tasks/progress.md`、本文件；模块未满足全部验收前不得标记完成。
3. 不得向公共头文件引入 OpenGL/GLAD 类型，也不得暴露底层句柄。
4. 保持 Backend 与 UI 解耦；后续任务不得擅自引入 Qt、WGL、EGL 或真实 Context 创建。
5. 遵守 RAII 与 Context 线程约束，析构不抛。
6. 在没有真实 Context 基础设施时继续采用注入 Fake 操作表验证；真实 Context 用例必须明确 Skipped，不能伪造渲染成功。
7. 修改 `IGlDrawOperations` 时，要同步更新所有 Fake 实现：至少 `OpenGLBackendLifecycleTests.cpp`、`OpenGLDrawTests.cpp`、`OpenGLShadingTests.cpp`、`OpenGLResizeTests.cpp`。
8. 验证后执行 `git diff --check`、`git status --short`，并清理任务专用 build 目录与临时 DLL。
9. 没有主人明确要求，不创建 Git commit；如果 Git 状态与本文件冲突，以实际 `git status` / `git log` 为准并在继续前说明。

## 11. 建议的新会话首轮操作

```powershell
Get-Content -Raw -Encoding UTF8 agent.md
Get-Content -Raw -Encoding UTF8 上下文.md
Get-Content -Raw -Encoding UTF8 docs/tasks/opengl-renderer.md
git status --short
git log -5 --oneline
```

然后根据主人指定的目标，从 `docs/tasks/progress.md` 中选择剩余 Phase 1 工作继续。OpenGL Renderer 已完成；不得自行假设下一模块或提前勾选 Phase 1 门禁，一旦存在不明确设计选择，先提问。
## 5.2 QT-005 EngineUiAdapter 与验证

截至 2026-09-03，已执行 `qt-application.md` 的 QT-005：

- 公共 Engine 协议新增 `include/dzc/InputEvent.h` 和 `SubmitInputCommand`；输入事件校验覆盖六类事件、归一化指针坐标、有限滚轮值、稳定按钮/修饰位及 Key/Focus/ResetRequest 结构。`Engine::applyCommand()` 能消费输入命令，但当前不注入或调用 Camera Controller，不改变相机状态。
- 新增 `src/app/EngineUiAdapter.h/.cpp`。Adapter 使用 QObject + Pimpl；App 内部 `IEngineUiPort` 不依赖 Qt，生产实现桥接 `Engine`，测试注入 Fake Port。支持 QString UTF-8 路径、QColor RGBA、点大小/着色/固定色/背景色/CUDA/ResetView、PointerMove/PointerButton/Wheel/Key/Focus/ResetRequest，以及 `currentSnapshot()` 和 `pollEvents()`。
- Qt 输入固定使用 widget 本地像素坐标、视口左上角原点和 `[0,1]` 归一化；左键/右键编码 `0/2`，修饰位 Shift/Ctrl/Alt/Meta 为 `1/2/4/8`，键盘使用有限 USB HID 映射。非法输入同步失败且不入队，队列或 Engine 错误原样返回；Adapter 不负责 Engine 生命周期、帧驱动或定时器。
- 新增 `tests/unit/EngineUiAdapterTests.cpp` 并注册 `dzc_engine_ui_adapter`（标签 `unit;app;qt;qt005`）；覆盖 Qt 值转换、错误不入队、六类输入、稳定编码、命令顺序、Snapshot/Event 读取、提交失败、公共 variant/合法性及 Engine 消费后的相机状态不变。
- 当前 Git 状态以实际 `git status --short` 为准：QT-005 源码、测试和文档均为工作树改动；`build-qt005/` 为任务专用未跟踪构建目录；本任务未创建 Git commit。
- 构建命令：`cmake -S . -B build-qt005 -DDZC_ENABLE_OPENGL=ON -DDZC_ENABLE_VULKAN=OFF -DDZC_ENABLE_CUDA=OFF -DDZC_BUILD_TESTS=ON`，随后构建受影响目标。QT-005 回归命令：`ctest --test-dir build-qt005 -C Debug -R 'dzc_engine_ui_adapter|dzc_engine_command|dzc_engine_public_api|dzc_command_line_options|dzc_settings_controller' --output-on-failure`，结果 5/5 通过。
- Qt 运行规则：仅允许从仓库根目录 `dll/` 使用已有 Qt DLL；本次测试使用其临时副本，测试后应清理 `build-qt005/tests/unit/Debug` 内临时 Qt DLL，不从 Qt 安装目录、其他构建目录或 PATH 复制。
- 遗留边界：QT-007 至 QT-012、Qt Application 模块级验收、Engine Controller 注入/相机行为转发和 Phase 1 阶段门禁仍未完成。
## 5.3 QT-006 文件加载与取消 UI

截至 2026-09-04，已执行 `qt-application.md` 的 QT-006：

- `MainWindow` 通过外部注入的 `EngineUiAdapter` 提供 PCD/PLY 文件选择入口；文件对话框过滤器不包含任意文件，路径转换和命令提交保持在 App 适配边界内，不读取或解析真实点云文件。
- 加载只入队 `LoadDatasetCommand`；UI 显示 `Not started`、`Loading`、`Completed`、`Cancelled` 和 `Failed` 状态，进度和点数来自 `EngineSnapshot`，离散进度/完成/取消/错误来自 `EngineEvent`。
- 取消按钮仅在 Snapshot 发布有效 `DatasetId` 后启用，并提交 `CancelDatasetLoadCommand`；取消、完成或失败后恢复重新加载。终态 DatasetId 会被记录，用于忽略旧任务的过期 Snapshot。
- 失败摘要使用用户可读错误消息，诊断信息追加到现有只读日志；未引入 Qt 到公共 Engine API、PCL、GPU 句柄或真实后台读取逻辑。
- 新增 `tests/ui/DatasetLoadUiTests.cpp` 并注册 `dzc_dataset_load_ui`（标签 `ui;app;qt;qt006`）；覆盖 UTF-8 路径、PCD/PLY 过滤、Snapshot/Event 进度、取消 DatasetId、完成/取消/失败、入队失败、空路径、失败后重载和过期 Snapshot。
- `build-qt006` 使用 Qt 5.15.19、OpenGL ON、Vulkan/CUDA OFF、Tests ON；Debug 构建成功，`dzc_dataset_load_ui`、`dzc_main_window_smoke`、`dzc_qt_application_smoke`、`dzc_engine_ui_adapter`、`dzc_engine_command`、`dzc_engine_public_api`、`dzc_command_line_options`、`dzc_settings_controller` 共 8/8 通过。Qt 测试运行时仅使用仓库根目录 `dll/` 的临时副本，收尾时清理任务构建目录和临时 DLL。
- QT-007 至 QT-012、Qt Application 模块级验收、Engine Controller 注入/相机行为转发和 Phase 1 阶段门禁仍未完成。
## 5.4 QT-007 渲染参数 UI 与验证

截至 2026-09-04，已执行 `qt-application.md` 的 QT-007：

- `MainWindow` 的 Render Parameters Dock 已提供点大小（`1.0` 至 `64.0`、步长 `1.0`）、`OriginalColor/FixedColor/Height/Intensity` 四种着色、RGBA 固定颜色和背景颜色按钮，以及 CUDA `Off/On/Auto` 三态选择；稳定 object name 为 `pointSizeControl`、`shadingModeControl`、`fixedColorButton`、`backgroundColorButton`、`cudaModeControl`。
- 所有控件变化均通过 `EngineUiAdapter` 提交现有命令；入队失败时控件回滚，状态栏显示用户摘要，现有只读日志追加诊断信息。设置保存失败不撤销已入队命令。无 Adapter 构造只恢复控件，不创建 Engine、线程或帧驱动。
- 公共 Qt-free Snapshot 增加 `EngineSnapshot::cudaMode`、`DatasetSummary::hasRgb` 和 `DatasetSummary::hasIntensity`。能力 `std::nullopt` 表示未知并保持着色项可选，`false` 才禁用对应项；`cudaAvailable` 只控制 CUDA 控件可用性，三态当前值以 Snapshot 的 `cudaMode` 为准。无真实 Reader 元数据时数据集能力保持未知。
- QSettings 固定组织名 `Dzc`、应用名 `Dzc-RenderEngine`，键为 `render/pointSize`、`render/shadingMode`、`render/fixedColor`、`render/backgroundColor`、`engine/cuda`；颜色严格使用 `#AARRGGBB`，非法或缺失值回退到点大小 `1.0`、OriginalColor、黑色固定/背景色和 CUDA Auto。
- 新增 `tests/ui/RenderParametersUiTests.cpp` 并注册 `dzc_render_parameters_ui`，使用 Fake Port 覆盖控件命令、Snapshot 能力、失败回滚、设置 round-trip、非法默认值和启动恢复；未访问真实点云文件、未解析点云、未创建 PCL/GPU 资源或 Engine 后台线程。
- 验证：`build-qt007` 配置成功，`dzc_app`、QT-007/QT-006/QT-004 smoke、QT-005 适配器和相关 Engine 目标 Release 构建成功；非 Qt `dzc_engine_command_tests`、`dzc_engine_public_api_tests`、`dzc_command_line_options_tests` 为 3/3 通过。Qt UI/Settings 测试因仓库根目录 `dll/` 的 Qt5Core 依赖 `icuin66.dll`、`icuuc66.dll`、`zstd.dll` 缺失而在加载阶段停止，未从外部 Qt 安装、PATH 或其他构建目录补 DLL，故未将其记录为通过。
- QT-008 至 QT-012、Qt Application 模块级验收、Engine Controller 注入/相机行为转发和 Phase 1 阶段门禁仍未完成。
## 5.5 后续任务编译测试责任流程（2026-09-04）

主人已明确调整后续协作流程：

- 后续所有任务由主人使用 VS2026/CMake 自行选择配置、构建目录、生成器和测试方式，并负责编译整个项目及运行测试。
- AI 不执行 CMake configure、CMake build、CTest、单元测试、UI 测试、集成测试，也不为测试复制或整理 Qt/PCL/VTK 等运行时 DLL。
- AI 可以进行源码阅读、依赖搜索、文档检查、静态分析和 `git diff --check`，但这些检查不能替代主人验证。
- 代码实现完成后，任务状态记录为“已实现，待主人验证”；只有主人明确确认编译测试通过后，才更新为“已完成”、勾选任务并进入下一个功能。
- 若主人报告编译或测试失败，AI 根据主人提供的日志和现象修复，修复后继续等待主人重新验证，不代报通过。
- 当前 QT-007 的代码实现记录和既有历史测试记录保留；主人已于 2026-09-05 明确确认自行编译测试通过，QT-007 已标记为已完成，现在可以进入 QT-008。
## 5.6 QT-007 主人验证完成（2026-09-05）

- 主人已使用 VS2026/CMake 自行完成 QT-007 的编译和测试，并明确确认测试通过。
- QT-007 已从“已实现，待主人验证”更新为“已完成”，任务 checklist 已勾选。
- 此次只同步主人提供的最终通过结论，不补写未提供的具体构建命令、测试筛选器或 DLL 运行时细节；此前测试环境 DLL 缺失的历史记录保留。
- 现在允许开始 QT-008；QT-008 至 QT-012、Qt Application 模块级验收和 Phase 1 门禁仍未完成。
## 5.7 QT-008 主人验证完成（2026-09-05）

- 主人已明确确认 QT-008 的编译和测试通过。
- QT-008 已在 `docs/tasks/qt-application.md` 标记为“已完成（2026-09-05）”并勾选。
- 本次只记录主人提供的通过结论，不补写具体构建命令、测试筛选器或 DLL 操作细节。
- QT-009 随后开始实施；Qt Application 模块继续保持“进行中”。

## 5.8 QT-009 OpenGL 宿主实现待主人验证（2026-09-05）

- 已实现外部 Engine/EngineUiAdapter 所有权、Engine 后端依赖注入、OpenGL 4.5 Core 默认格式、Qt Context Bridge 和 OpenGLRenderWidget 生命周期转发。
- 已实现初始化/帧错误一次性保护、隐藏/最小化停止连续刷新、恢复可见后请求刷新，以及 Context 有效时的 Engine shutdown。
- QT-009 不生成 RenderFrame、不实现真实点云绘制、不创建 Engine 线程、定时器、Reader、PCL 或 GPU 对象。
- 已新增 QT-009 UI 测试目标和无图形环境 Skipped 约定。

## 5.9 QT-009 主人验证完成（2026-09-05）

- 主人已明确确认 QT-009 的编译和测试通过。
- QT-009 已在 `docs/tasks/qt-application.md` 标记为“已完成（2026-09-05）”并勾选。
- 本次只记录主人提供的通过结论，不补写具体构建命令、测试筛选器或 DLL 操作细节。
- QT-010 至 QT-012 仍未开始；Qt Application 模块继续保持“进行中”。
